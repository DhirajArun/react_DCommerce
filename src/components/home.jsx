import React, { Component } from "react";
import SProducts from "./sProducts";

class Home extends Component {
  state = {};
  render() {
    return (
      <React.Fragment>
        <SProducts />;
      </React.Fragment>
    );
  }
}

export default Home;
